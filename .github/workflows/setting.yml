name: Learn Ui workflow

on: [pull_request]

jobs:
  first:
    runs-on: 'ubuntu-latest'
    steps:
      - run: echo 'hi'
      - name: checkout branch
        uses: actions/checkout@v2
        run: ls
